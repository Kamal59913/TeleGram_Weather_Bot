// This file is not used by the app. The app uses its own copy at apps/admin/src/layout/AppLayout.tsx
import { Outlet } from "react-router";

const AppLayout: React.FC = () => {
  return <Outlet />;
};

export default AppLayout;
