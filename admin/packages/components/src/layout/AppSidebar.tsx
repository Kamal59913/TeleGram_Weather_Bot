// This file is not used by the app. The app uses its own copy at apps/admin/src/layout/AppSidebar.tsx
// Kept as a placeholder to avoid import errors from other package files.

const AppSidebar: React.FC = () => {
  return null;
};

export default AppSidebar;
